# 단타 다종목 시드 배분 및 시드 초과 후보 숨김 정비

- **ID**: 005
- **날짜**: 2026-04-15
- **유형**: 기능 추가 / 버그 수정

## 작업 요약
자동순환이 남은 시드를 종목별로 분산해 쓰지 못하고 사실상 한 종목 위주로만 점검하던 흐름을 수정했다. 총 시드 대비 사용 중인 시드와 남은 시드를 계산해 신규 슬롯별 시드를 동적으로 배분하고, 슬롯당 1주도 못 사는 종목은 추천/자동순환 후보에서 숨기도록 정리했다.

## 변경 파일 목록
### 백엔드
- `src/portal/trading/model/struct/daytrade_engine.py`
  - `shared_budget_status()`에 총 시드 / 사용 시드 / 남은 시드 / 슬롯 수 / 슬롯당 시드 추가
  - `auto_candidates()`에서 남은 시드와 슬롯 수 기준으로 후보 필터링 및 시드 초과 후보 제외
  - `auto_cycle()`에서 남은 시드를 종목별로 배정하며 순차 실행하도록 보정

### 프론트엔드
- `src/app/page.daytrade/view.ts`
  - 시드 초과 종목을 랭킹에서 숨기는 필터 추가
  - 추천 선택 시 현재 시드 안에 들어오는 첫 후보를 우선 선택

## 검증
- 빌드 성공 확인
- `bootstrap` 응답에서 `total_seed_krw`, `used_seed_krw`, `remaining_seed_krw`, `per_symbol_seed_krw` 정상 확인
