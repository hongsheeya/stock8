# Codex Instructions

Follow `.github/copilot-instructions.md`.

Before code changes, if `.github/custom/custom-instructions.md` exists, read it first. If that file lists reference files, read the relevant referenced files too.

For WIZ work:
- Use WIZ MCP tools first.
- Start with `wiz_workspace_status`.
- Modify only the current WIZ project.
- Use `wiz_source_*`, `wiz_package_*`, and `wiz_project_*` according to the target path.
